{
  "name": "book-alert-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub-only backend for fetching engineering book releases from Amazon PA-API and publishing JSON to GitHub Pages.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/generateBooksJson.js",
    "test": "node test/fetchBooks.test.js",
    "check": "node test/fetchBooks.test.js && node scripts/generateBooksJson.js --sample"
  }
}
